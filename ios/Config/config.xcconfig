PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_IDENTIFIER)$(BUNDLE_IDENTIFIER_APPEND)

BUNDLE_DISPLAY_NAME = controle
BUNDLE_IDENTIFIER = com.controle.mobile

RUNTIME_URL = https:\/\/www.carrepairdelfzijl.nl/controle
